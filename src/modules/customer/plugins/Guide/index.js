import Core from '@core_modules/customer/plugins/Guide/core';
import Content from '@core_modules/customer/plugins/Guide/components';

const DefaultAddressForm = (props) => (
    <Core {...props} Content={Content} />
);

export default DefaultAddressForm;
