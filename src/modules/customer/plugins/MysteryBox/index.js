import Core from '@core_modules/customer/plugins/MysteryBox/core';
import Content from '@core_modules/customer/plugins/MysteryBox/components';

const Page = (props) => (<Core {...props} Content={Content} />);

export default Page;
